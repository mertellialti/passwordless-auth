import { VerifyRegistrationResponse } from './verify-registration-response';

describe('VerifyRegistrationResponse', () => {
  it('should be defined', () => {
    expect(new VerifyRegistrationResponse()).toBeDefined();
  });
});
