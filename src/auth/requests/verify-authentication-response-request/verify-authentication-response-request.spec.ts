import { VerifyAuthenticationResponseRequest } from './verify-authentication-response-request';

describe('VerifyAuthenticationResponseRequest', () => {
  it('should be defined', () => {
    expect(new VerifyAuthenticationResponseRequest()).toBeDefined();
  });
});
